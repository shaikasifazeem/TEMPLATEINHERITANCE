from django.shortcuts import render
from django.http import HttpResponse
def sql(request):
    return HttpResponse('sql is software programing language it isused to store data')

# Create your views here.
